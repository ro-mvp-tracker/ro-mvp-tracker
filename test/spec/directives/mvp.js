'use strict';

describe('Directive: mvp', function () {

  // load the directive's module
  beforeEach(module('roMvpTrackerApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
